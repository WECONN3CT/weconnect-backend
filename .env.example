# ============================================
# WeConnect Backend - Umgebungsvariablen
# ============================================
# Kopiere diese Datei nach .env und passe die Werte an!

# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL/Supabase)
# WICHTIG: Verwende in Produktion eine sichere Verbindung!
DATABASE_URL=postgresql://user:password@host:5432/database

# JWT Authentifizierung
# WICHTIG: Generiere ein sicheres Secret (min. 32 Zeichen)!
# Beispiel: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRES_IN=1d

# n8n Webhook URL
N8N_WEBHOOK_URL=http://localhost:5678/webhook

# Webhook Security
# WICHTIG: Generiere ein sicheres Secret für Webhook-Authentifizierung!
# Beispiel: openssl rand -hex 32
WEBHOOK_SECRET=CHANGE_THIS_TO_A_SECURE_WEBHOOK_SECRET

# Frontend URL (für CORS)
FRONTEND_URL=http://localhost:5173
